[pytest]
testpaths = tests
python_files = test_*.py
# Prevent pytest from recursing into directories with non-test scripts or large artifacts
norecursedirs = backend scripts public node_modules .venv venv dist .git
